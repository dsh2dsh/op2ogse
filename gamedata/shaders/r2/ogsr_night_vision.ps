#include "common.h"

uniform float4 pnv_color;

float4 main(p_screen _in) : COLOR
{
    float3 col = tex2D(s_image, _in.tc0);
    float lum = dot(col, LUMINANCE_VECTOR * pnv_color.w);
    col = pnv_color.xyz * lum;

    return float4(col, 1.);
}
