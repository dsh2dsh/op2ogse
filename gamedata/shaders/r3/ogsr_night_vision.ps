#include "common.h"

uniform float4 pnv_color;

float4 main(p_screen _in) : SV_Target
{
    float3 col = s_image.Sample(smp_rtlinear, _in.tc0);
    float lum = dot(col, LUMINANCE_VECTOR * pnv_color.w);
    col = pnv_color.xyz * lum;

    return float4(col, 1.);
}
